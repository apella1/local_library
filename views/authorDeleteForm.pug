extends layout

block content
  h1(class="text-4xl font-bold") #{title}: #{author.name}
  p(class="mb-4 italic") #{author.dobFormatted} - #{author.dodFormatted}

  if authorBooks.length

    p(class="text-lg font-semibold") Delete the following books before attempting to delete this author.

    div(style='margin-left:20px;margin-top:20px')

      h4(class="text-2xl text-blue-700") Books

      dl
      each book in authorBooks
        dt(class="p-2")
          a(href=book.url class="text-lg text-blue-500 hover:underline") #{book.title}
        dd(class="pl-3 font-normal text-md") #{book.summary}

  else
    p(class="m-0 text-lg") Do you really want to delete this Author?

    form(method='POST' action='' class="flex flex-col space-y-2")
      div(class="p-8 m-4")
        input#authorId.form-control(type='hidden',name='authorId', required='true', value=author._id )

      button(type='submit' class="self-center p-2 text-white bg-blue-700 rounded-md hover:bg-blue-500") Delete Author
