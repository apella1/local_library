extends layout

block content
		h1= title
		
		ul
				each genre in genreList
						li
								a(href=genre.url) #{genre.category}
				else
						li
								There are no genres specified in the library

