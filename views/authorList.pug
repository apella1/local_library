extends layout

block content
		h1= title
		
		ul
				each author in authorList
						li
								a(href=author.url) #{author.name}
								| (#{author.dobFormatted} - #{author.dodFormatted})

				else
						li
								There are no author records
